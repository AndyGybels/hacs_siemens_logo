{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Siemens LOGO!",
        "data": {
          "host": "IP Address",
          "rack": "Rack",
          "slot": "Slot",
          "model": "LOGO! Model",
          "scan_interval": "Poll Interval (ms)"
        },
        "data_description": {
          "scan_interval": "How often to read the PLC state in milliseconds. Minimum 100ms. Setting this too low can overload the connection and cause the integration to stop working."
        }
      },
      "entities": {
        "title": "Configure Entities",
        "description": "Enter LOGO! block addresses separated by commas.\nExample: {example}",
        "data": {
          "entities": "Entities"
        }
      },
      "addresses": {
        "title": "VM Addresses",
        "description": "Set the address, friendly name and optional unique ID for each entity. Address format: byte.bit for digital (e.g. 0.0), byte for analog (e.g. 1032). Leave unique ID blank to use the auto-generated one."
      },
      "reconfigure": {
        "title": "Reconfigure LOGO! connection",
        "data": {
          "host": "IP Address",
          "rack": "Rack",
          "slot": "Slot",
          "model": "LOGO! Model",
          "scan_interval": "Poll Interval (ms)"
        },
        "data_description": {
          "scan_interval": "How often to read the PLC state in milliseconds. Minimum 100ms. Setting this too low can overload the connection and cause the integration to stop working."
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to LOGO! at the specified address",
      "invalid_entity": "Invalid entity address. Use format like NI1, AI3, Q1",
      "invalid_address": "Invalid address format. Use byte.bit for digital (e.g. 0.0) or byte for analog."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Connection Settings",
        "data": {
          "host": "IP Address",
          "rack": "Rack",
          "slot": "Slot",
          "model": "LOGO! Model",
          "scan_interval": "Poll Interval (ms)"
        },
        "data_description": {
          "scan_interval": "How often to read the PLC state in milliseconds. Minimum 100ms. Setting this too low can overload the connection and cause the integration to stop working."
        }
      },
      "entities": {
        "title": "Configure Entities",
        "description": "Enter LOGO! block addresses separated by commas.\nExample: {example}",
        "data": {
          "entities": "Entities"
        }
      },
      "addresses": {
        "title": "VM Addresses",
        "description": "Set the address, friendly name and optional unique ID for each entity. Address format: byte.bit for digital (e.g. 0.0), byte for analog (e.g. 1032). Leave unique ID blank to use the auto-generated one."
      }
    }
  }
}
